name: Tests

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ main ]

jobs:
  unit-tests:
    name: Run unit tests (.NET)
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore dependencies
        run: dotnet restore Tests/TerraDrive.Tests/TerraDrive.Tests.csproj

      - name: Build
        run: dotnet build Tests/TerraDrive.Tests/TerraDrive.Tests.csproj --no-restore

      - name: Run tests
        run: dotnet test Tests/TerraDrive.Tests/TerraDrive.Tests.csproj --no-build --verbosity normal --filter "Category!=Integration"

  osm-downloader-build:
    name: Build OsmDownloader tool (.NET)
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore dependencies
        run: dotnet restore Tools/OsmDownloader/OsmDownloader.csproj

      - name: Build
        run: dotnet build Tools/OsmDownloader/OsmDownloader.csproj --no-restore
